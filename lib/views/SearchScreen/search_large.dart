import 'package:flutter/material.dart';

class SearchLarge extends StatelessWidget {
  const SearchLarge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}